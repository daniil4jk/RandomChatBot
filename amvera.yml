meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    out/*.jar: /
  args: spring-boot:repackage
run:
  jarName: myFirstBot.jar
  persistenceMount: /data
  containerPort: 8084
