meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    out/*.jar: /
  args: spring-boot:repackage
run:
  jarName: bot.jar
  persistenceMount: /data
  containerPort: 8084
